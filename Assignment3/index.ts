//Declare a variable for storing your favorite movie name with a string type annotation.

const favoriteMovie = "Harry Poetry";

//Create a template string to introduce yourself, including your name and age.

const age = 25;
const fullname = "Jahanzaib Tayyab";
console.log(`Hello my name is ${fullname} and i am ${age} years old`);

const intruduction = `Hello my name is  Jahanzaib  and i am 25 years old`;

//nitialize a constant with a type annotation for your current mood as a boolean

const currentMood: boolean = true;

//Combine a user's first and last name using template literals and log the full name.
const first = "Jahanzaib";
const last = "Tayyab";
console.log(`${first} ${last}`);
